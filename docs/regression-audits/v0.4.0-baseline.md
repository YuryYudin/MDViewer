# v0.4.0 baseline audit

Generated by `scripts/audit-v040.mjs` against v0.4.0 on 2026-05-14T14:47:28.446Z.

Each row corresponds to one `##`-level section of `e2e/fixtures/render-gallery.md`.
Comparison is whitespace-normalized (`/\s+/` collapsed to a single space).

- `render_markdown` signature parity: `pub fn render_markdown(source: &str, opts: &RenderOptions) -> RenderResult {`
- `RenderOptions::default()` field-set parity: identical
- v0.4.0 render-cli build: ok

> **Known benign divergence:** syntect grammar and theme versions may rev between v0.4.0 and current main. HTML differences inside `<pre><code>` bodies are expected and do NOT count as regressions; the per-row commentary calls this out where relevant.

## Headings

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Unordered list

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Ordered list

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Blockquote

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Fenced code

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Mermaid

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Table

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Inline marks

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Link

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Inline image

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Long line (word-wrap exercise)

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

## Selectable phrases

**Parity:** View-mode HTML matches v0.4.0 (whitespace-normalized).

