name: Release

# Triggered on `v*` tag push so a typo'd commit on `main` cannot publish.
# `workflow_dispatch` lets a release manager fire the pipeline manually for
# a pre-release smoke check (the resulting Release stays in draft state).
#
# All `${{ ... }}` interpolations pull from the matrix block (config values
# committed here) or `github.ref_name` (the tag name). They flow into
# action inputs (`with:`), not shell-interpolated `run:` commands, so
# command-injection vectors via tag names don't apply.
on:
  push:
    tags: ['v*']
  workflow_dispatch:

# tauri-action needs `contents: write` to create a Release and attach
# bundle artifacts. No other scopes required.
permissions:
  contents: write

jobs:
  bundle:
    name: Bundle (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          # Apple Silicon only (Intel macs deferred per the design discussion;
          # revisit if user reports indicate Intel is a meaningful share).
          - os: macos-14
            target: aarch64-apple-darwin
            bundles: dmg
            label: macOS (Apple Silicon)
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            bundles: msi
            label: Windows
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            bundles: deb,appimage,rpm
            label: Linux
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: 'npm'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      # webkit2gtk-4.1 is the Tauri 2 line; rpm-build (`rpm` package) is
      # needed for the rpm bundle target. Linux-only.
      - name: Linux build deps
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libsoup-3.0-dev \
            libssl-dev \
            build-essential \
            libgtk-3-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            libxdo-dev \
            rpm

      - run: npm ci

      # A12: build the mdviewer-askpass helper bin BEFORE tauri build runs
      # so the `bundle.externalBin` declaration in tauri.conf.json can
      # resolve `binaries/mdviewer-askpass-<triple>` at bundle time. The
      # outer build.rs handles this in CI/dev runs, but the release path
      # invokes tauri-action which forces --release; we mirror that
      # explicitly here to guarantee a real signed binary lands in
      # `src-tauri/binaries/` rather than the empty placeholder build.rs
      # writes on a fresh checkout.
      #
      # Windows uses the russh in-process auth callback (no helper bin
      # ever invoked), so the step is skipped there — the placeholder
      # written by build.rs is sufficient for the MSI bundler.
      - name: Build mdviewer-askpass helper
        working-directory: src-tauri
        shell: bash
        run: |
          # The helper bin has a #[cfg(unix)] real implementation and a
          # #[cfg(not(unix))] stub `main` that exits non-zero — so the bin
          # target compiles on every platform. We build it on Windows too
          # because Tauri's MSI bundler (WiX `light.exe`) rejects 0-byte
          # externalBin placeholders, and there's no way to drop a platform-
          # specific externalBin entry from a single tauri.conf.json.
          #
          # cargo runs from src-tauri but writes to the workspace's
          # target/ directory at the repo root — hence the `../target/`
          # prefix on the cp source path. Windows artifacts have `.exe`.
          cargo build --release --bin mdviewer-askpass --locked --target ${{ matrix.target }}
          mkdir -p binaries
          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            cp "../target/${{ matrix.target }}/release/mdviewer-askpass.exe" \
               "binaries/mdviewer-askpass-${{ matrix.target }}.exe"
          else
            cp "../target/${{ matrix.target }}/release/mdviewer-askpass" \
               "binaries/mdviewer-askpass-${{ matrix.target }}"
          fi

      # tauri-action runs `tauri build`, picks up bundle.targets from
      # tauri.conf.json (overridden via --bundles), and uploads each
      # produced artifact to a draft GitHub Release named after the tag.
      # Code-signing secrets intentionally NOT wired (unsigned bundles
      # work, but trigger Gatekeeper / SmartScreen warnings on first
      # launch). Add APPLE_CERTIFICATE / APPLE_ID + signing flags here
      # when notarization is in scope.
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'MDViewer ${{ github.ref_name }}'
          releaseDraft: true
          prerelease: false
          args: --target ${{ matrix.target }} --bundles ${{ matrix.bundles }}

  # Android sideload APK. Runs in parallel with the desktop bundle matrix so
  # cold tag pushes finish in roughly one bundle's wall-clock time. The
  # signed APK lands as an asset on the SAME draft Release `tauri-action`
  # creates from the bundle matrix; both job sets must finish green before
  # a human flips the draft to Published.
  #
  # Why a separate job (vs. reusing `.github/workflows/android.yml`'s
  # `release-apk`): release.yml is the single source of truth for what
  # ships on a tag. android.yml's `release-apk` job is the per-PR /
  # per-commit signing rehearsal; duplicating the assemble step here keeps
  # the release-cut path visible in one file. The NDK + cargo-ndk + signing
  # env-var contract matches android.yml exactly — drift will be caught by
  # E4's `cleanupReleaseKeystore` finalizer and the leaked-keystore guard.
  android:
    name: Bundle (Android)
    runs-on: ubuntu-latest
    # Pin to the firebase-distribution environment so the Upload-to-Firebase
    # step below can read FIREBASE_ANDROID_APP_ID / FIREBASE_SERVICE_ACCOUNT
    # / FIREBASE_TESTER_GROUP. The environment's branch policy restricts
    # deployment to `main`, which matches tag pushes (tags from main).
    environment: firebase-distribution
    steps:
      - uses: actions/checkout@v6
        with:
          # versionName resolves via `git describe --tags --abbrev=0` in
          # `app/build.gradle.kts`. Default depth=1 has no tags, so the
          # resulting APK would be labelled `0.0.0-dev`. Mirrors the
          # checkout in android.yml's release-apk job.
          fetch-depth: 0

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - uses: android-actions/setup-android@v3

      - name: Install AGP-compatible NDK + cmake
        run: |
          # Mirrors android.yml's install step: gradle's NDK selection
          # in android/core/build.gradle.kts globs $ANDROID_HOME/ndk/
          # and picks the highest AGP-supported version installed,
          # falling back to a hard pin when nothing compatible is
          # there. The runner image's preinstalled NDK is too-new
          # preview, so we install the fallback version explicitly to
          # ensure gradle finds it. Bump in lockstep with the gradle
          # fallback when bumping AGP.
          NDK_VERSION="27.2.12479018"
          sdkmanager --install "ndk;$NDK_VERSION" "cmake;3.22.1" >/dev/null
          ndk_dir="$ANDROID_HOME/ndk/$NDK_VERSION"
          if [ ! -d "$ndk_dir" ]; then
            echo "::error::NDK install failed: $ndk_dir not present after sdkmanager"
            exit 1
          fi
          echo "ANDROID_NDK_HOME=$ndk_dir" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=$ndk_dir" >> "$GITHUB_ENV"
          echo "Resolved NDK: $ndk_dir"

      - name: Assemble release APK
        working-directory: android
        env:
          ANDROID_RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
          ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          ANDROID_RELEASE_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEYSTORE_PASSWORD }}
          ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: ./gradlew :app:assembleRelease --stacktrace

      # Mirror the leaked-keystore guard from android.yml. The
      # cleanupReleaseKeystore finalizer in app/build.gradle.kts runs on
      # both success and failure, but a defense-in-depth check here means
      # the release-cut path can't ship an APK alongside a tmp keystore.
      - name: Verify tmp keystore was cleaned up
        working-directory: android
        run: |
          if [ -f app/build/release-keystore.jks ]; then
            echo "::error::Release keystore tmp file leaked into workspace"
            exit 1
          fi

      # Rename so the asset on the Release matches the desktop bundle
      # naming (`MDViewer_<ver>_*`). Using the raw tag (`v0.2.0`) keeps the
      # filename traceable to a specific Release page. The final asset
      # filename is `mdviewer-android-<tag>.apk` (e.g.
      # `mdviewer-android.apk`-style — see RELEASING.md "Android sideload").
      - name: Rename APK to release asset name
        run: |
          mv android/app/build/outputs/apk/release/app-release.apk \
             "android/app/build/outputs/apk/release/mdviewer-android-${{ github.ref_name }}.apk"

      # Block the v0.4.2–v0.4.9 footgun before it can reach testers again.
      # Each of those releases shipped an APK signed with the well-known
      # default Android Studio debug cert because the gradle release-signing
      # block silently fell back to `keystore/debug.keystore` when the
      # ANDROID_RELEASE_KEYSTORE_BASE64 secret was missing. Firebase App
      # Distribution's AppTester refuses to install APKs signed with that
      # cert (the cert is identical across every developer's machine, so
      # it grants no provenance) — tester-visible "install failed". The
      # gradle-side guard in app/build.gradle.kts now throws on CI without
      # the secret, but this step is a belt-and-braces check: even if some
      # future variant bypasses gradle's signing config, an APK signed by
      # `CN=Android Debug` fails the build before the smoke step runs.
      - name: Verify APK is release-signed (reject debug cert)
        run: |
          set -euo pipefail
          APKSIGNER="$(find "$ANDROID_SDK_ROOT/build-tools" -name apksigner -type f | sort -V | tail -1)"
          APK="android/app/build/outputs/apk/release/mdviewer-android-${{ github.ref_name }}.apk"
          if [ -z "$APKSIGNER" ]; then
            echo "::error::apksigner not found under \$ANDROID_SDK_ROOT/build-tools"
            exit 1
          fi
          # `apksigner verify --print-certs` writes the Signer DN line to
          # STDOUT, but a sibling warning line may land on STDERR — the v0.4.12
          # run captured an empty CERT_DN because the grep ran before stderr
          # was redirected reliably. Empty DN means "we couldn't even READ the
          # cert" and must be a hard failure, otherwise the inner debug-cert
          # check silently no-ops and any cert (including the default debug
          # one) sails through. Capture stdout+stderr together, require a
          # non-empty DN, AND match the debug-cert pattern.
          APKSIG_OUT="$("$APKSIGNER" verify --print-certs "$APK" 2>&1)"
          # apksigner's "Signer DN" line format depends on build-tools version:
          #   build-tools <= 34: `Signer #1 certificate DN: CN=...`
          #   build-tools >= 35: `V2 Signer: certificate DN: CN=...`
          # Match by the stable suffix `certificate DN:` to handle both
          # (v0.4.13 ran on the newer format and the old regex no-matched).
          CERT_DN="$(echo "$APKSIG_OUT" | grep -m1 'certificate DN:' || true)"
          echo "APK signer DN: $CERT_DN"
          if [ -z "$CERT_DN" ]; then
            echo "::error::apksigner did not report a Signer #1 DN — cannot verify the APK is release-signed."
            echo "--- apksigner full output (for debugging) ---"
            echo "$APKSIG_OUT"
            echo "--- end apksigner output ---"
            exit 1
          fi
          if echo "$CERT_DN" | grep -qE 'CN=Android Debug'; then
            echo "::error::APK is signed with the default Android Studio debug cert."
            echo "::error::Firebase App Distribution rejects this — see android/app/build.gradle.kts release signingConfig."
            echo "::error::Provision ANDROID_RELEASE_KEYSTORE_BASE64 (+ password/alias/key-password) in the firebase-distribution environment."
            exit 1
          fi

      # Release-APK smoke test on an emulator. The signed release APK
      # differs from the debug APK in two material ways that the existing
      # android.yml emulator job can't catch:
      #   (1) R8 minification + ProGuard rules — a missing keep rule
      #       NoSuchMethodErrors only at release-build runtime;
      #   (2) signed release signingConfig — the wrong keystore alias or
      #       a stale signing config crashes on first launch with a
      #       crypto error.
      # This step boots an emulator (KVM-accelerated), installs the
      # signed APK, launches the LAUNCHER activity, waits 8s, then asserts
      # the app process is still alive. Captures logcat as an artifact
      # for post-mortem either way. Failure here BLOCKS Firebase
      # distribution — we don't want to ship a crashing APK to testers.
      - name: Enable KVM for emulator
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # Write the smoke-test script to a file in $GITHUB_WORKSPACE. This
      # sidesteps two reactivecircus/android-emulator-runner quirks:
      # (1) its `script:` input runs each line via `sh -c`, so shell
      # variables don't persist across lines; (2) the per-line `sh -c`
      # doesn't surface the inner script's stderr in a way `gh run view`
      # can render. Sourcing a single file via `bash <path>` keeps the
      # whole script in one process, and the file path under
      # $GITHUB_WORKSPACE stays accessible to the upload-artifact step.
      - name: Write smoke-test script
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          cat > "$GITHUB_WORKSPACE/smoke-release-apk.sh" <<'SCRIPT'
          #!/usr/bin/env bash
          set -euo pipefail
          REF_NAME="${REF_NAME:?REF_NAME not set}"
          APK="android/app/build/outputs/apk/release/mdviewer-android-${REF_NAME}.apk"
          LOG="${GITHUB_WORKSPACE}/release-apk-logcat.txt"

          echo "Installing signed release APK: $APK"
          ls -la "$APK"
          adb install -r -t "$APK"
          adb logcat -c

          # am start -W waits for the activity to surface; the exit code
          # only reflects launch dispatch, not eventual liveness — hence
          # the 8-second wait + ps check below.
          adb shell am start -W -n dev.mdviewer/.MainActivity
          echo "Waiting 8s for first-paint / late-crash window…"
          sleep 8
          adb logcat -d -t 600 > "$LOG" 2>&1 || true

          if adb shell 'ps -A | grep -w dev.mdviewer' | grep -q dev.mdviewer; then
            echo "::notice::Signed release APK launched and stayed alive for 8s — smoke test PASS."
            exit 0
          fi

          echo "::error::Signed release APK CRASHED within 8s of launch."
          echo "--- LOGCAT (last 200 lines) ---"
          tail -n 200 "$LOG" || true
          echo "--- END LOGCAT ---"
          if grep -q "FATAL EXCEPTION" "$LOG"; then
            echo "--- FATAL EXCEPTION context ---"
            grep -A 60 "FATAL EXCEPTION" "$LOG" || true
            echo "--- END FATAL EXCEPTION context ---"
          fi
          exit 1
          SCRIPT
          chmod +x "$GITHUB_WORKSPACE/smoke-release-apk.sh"

      - name: Release-APK smoke test (emulator)
        uses: reactivecircus/android-emulator-runner@v2
        env:
          REF_NAME: ${{ github.ref_name }}
        with:
          api-level: 33
          target: google_apis
          arch: x86_64
          profile: pixel_4
          # Disable hardware acceleration features that flake on CI
          # runners; the smoke test doesn't need GPU rendering.
          emulator-options: -no-window -no-audio -no-snapshot -no-boot-anim -accel on -gpu swiftshader_indirect
          disable-animations: true
          script: bash "$GITHUB_WORKSPACE/smoke-release-apk.sh"

      - name: Upload release-APK smoke logcat (always)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: release-apk-smoke-logcat
          path: ${{ github.workspace }}/release-apk-logcat.txt
          if-no-files-found: warn

      # Append the APK to the SAME draft Release `tauri-action` creates
      # from the bundle matrix. `softprops/action-gh-release@v2` is
      # idempotent — if the bundle matrix wins the race and creates the
      # draft first, this call attaches; if this job wins, it creates the
      # draft and tauri-action attaches the desktop bundles to it.
      # `draft: true` keeps the human Publish gate intact.
      - name: Upload signed APK to draft release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: 'MDViewer ${{ github.ref_name }}'
          draft: true
          prerelease: false
          files: android/app/build/outputs/apk/release/mdviewer-android-${{ github.ref_name }}.apk
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Push the same signed APK to Firebase App Distribution so testers
      # get an in-app update prompt without waiting for the human to flip
      # the GitHub draft to Published. The environment-scoped secrets
      # (firebase-distribution) provide:
      #   - FIREBASE_ANDROID_APP_ID — the App Distribution app identifier
      #     (1:<project_number>:android:<hash>)
      #   - FIREBASE_SERVICE_ACCOUNT — JSON service-account key with the
      #     `firebase.distributions.publish` role on this app
      #   - FIREBASE_TESTER_GROUP — the named tester group to distribute to
      # Release notes are pulled from the tag's annotated message via
      # `git for-each-ref` so testers see the same changelog GitHub shows.
      - name: Extract tag release notes for Firebase
        id: tag_notes
        run: |
          notes="$(git for-each-ref refs/tags/${{ github.ref_name }} --format='%(contents)' 2>/dev/null | head -c 1000)"
          if [ -z "$notes" ]; then
            notes="MDViewer ${{ github.ref_name }} — see https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }} for the full changelog."
          fi
          {
            echo "notes<<EOF"
            echo "$notes"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      # Sanity-check the Firebase secrets BEFORE handing them to the
      # distribution action — wzieba's action surfaces a generic "Failed
      # to authenticate, have you run firebase login?" on every failure
      # mode, which makes debugging the actual cause (wrong format, IAM
      # role missing, expired key) impossible from the logs alone.
      # This step prints non-sensitive diagnostics (first char, length,
      # key presence) so a malformed secret shows up immediately.
      - name: Sanity-check Firebase secrets
        env:
          SA_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          APP_ID: ${{ secrets.FIREBASE_ANDROID_APP_ID }}
          GROUP: ${{ secrets.FIREBASE_TESTER_GROUP }}
        run: |
          set -e
          if [ -z "$SA_JSON" ]; then echo "::error::FIREBASE_SERVICE_ACCOUNT is empty"; exit 1; fi
          if [ -z "$APP_ID" ]; then echo "::error::FIREBASE_ANDROID_APP_ID is empty"; exit 1; fi
          if [ -z "$GROUP" ]; then echo "::error::FIREBASE_TESTER_GROUP is empty"; exit 1; fi
          first_char="$(printf '%s' "$SA_JSON" | head -c 1)"
          if [ "$first_char" != "{" ]; then
            echo "::error::FIREBASE_SERVICE_ACCOUNT does not start with '{' (saw '$first_char'). Expected raw JSON — not base64, not a path. See https://github.com/YuryYudin/MDViewer/blob/main/.github/workflows/release.yml for the format guide."
            exit 1
          fi
          # Verify it's valid JSON without leaking content; jq is available on ubuntu-latest.
          if ! printf '%s' "$SA_JSON" | jq -e 'type == "object"' >/dev/null 2>&1; then
            echo "::error::FIREBASE_SERVICE_ACCOUNT is not valid JSON. Re-paste the service account key file content verbatim."
            exit 1
          fi
          # Verify expected fields are present (without printing values).
          for field in type project_id private_key client_email; do
            if ! printf '%s' "$SA_JSON" | jq -e ".$field // empty" >/dev/null 2>&1; then
              echo "::error::FIREBASE_SERVICE_ACCOUNT is missing required field: $field"
              exit 1
            fi
          done
          echo "FIREBASE_SERVICE_ACCOUNT looks well-formed (object with type/project_id/private_key/client_email)."
          echo "FIREBASE_ANDROID_APP_ID prefix: $(echo "$APP_ID" | cut -c1-6)…"
          echo "FIREBASE_TESTER_GROUP: $GROUP"

      - name: Upload signed APK to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_ANDROID_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          groups: ${{ secrets.FIREBASE_TESTER_GROUP }}
          file: android/app/build/outputs/apk/release/mdviewer-android-${{ github.ref_name }}.apk
          releaseNotes: ${{ steps.tag_notes.outputs.notes }}

  # Sentinel "the draft is complete" gate. `needs: [bundle, android]` means
  # this job only runs (and the workflow's overall status only goes green)
  # when EVERY desktop matrix leg AND the Android job have succeeded.
  # GitHub's commit-status / branch-protection UI surfaces this single
  # check, so a release manager sees one yes/no signal before clicking
  # **Publish release** in the GitHub UI. The job intentionally does no
  # publish action — it only summarises that the draft is ready.
  release-ready:
    name: Release draft ready (desktop + Android)
    runs-on: ubuntu-latest
    needs: [bundle, android]
    steps:
      - name: Confirm both bundle matrix and Android job succeeded
        run: |
          echo "Desktop bundles + Android APK all built and uploaded."
          echo "Draft Release ${{ github.ref_name }} is ready for human review."
          echo "Smoke-test per RELEASING.md, then click Publish in the GitHub UI."

  # Follow-up TODOs intentionally left here so reviewers see them in PRs:
  # - macOS notarization: APPLE_CERTIFICATE / APPLE_ID secrets + tauri-action's
  #   built-in signing flow. Deferred per the design — unsigned DMG works
  #   (right-click → Open on first launch).
  # - Windows EV cert: SignTool integration with a Comodo or Sectigo cert.
  #   Deferred per the same call. SmartScreen will warn on first run.
  # - Intel macOS: revisit if Intel users surface in real reports.
  # - Play Store upload: complement the GitHub Release sideload APK with an
  #   internal-track AAB upload via `r0adkll/upload-google-play`. Requires a
  #   Google Play service account JSON in CI secrets.
